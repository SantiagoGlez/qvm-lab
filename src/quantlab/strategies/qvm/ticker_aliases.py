# Tickers whose Yahoo Finance symbol differs from the canonical ticker.
# Add entries here for foreign-listed stocks or symbol differences.
YAHOO_TICKER_MAP: dict[str, str] = {
    "RNO": "RNO.PA",
}
